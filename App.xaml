<Application x:Class="CybersecurityAwarenessBot.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#CE93D8"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#7B1FA2"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#E1BEE7"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#1A0F2E"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#2D1B4E"/>
        <SolidColorBrush x:Key="InputBrush" Color="#3D2A5C"/>
        <SolidColorBrush x:Key="BotBubbleBrush" Color="#2D1B69"/>
        <SolidColorBrush x:Key="UserBubbleBrush" Color="#FF5E00"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#FF00AA"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#FF7B7012"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="#FF1744"/>
        <SolidColorBrush x:Key="WorriedBrush" Color="#00E5FF"/>
        <SolidColorBrush x:Key="CuriousBrush" Color="#FF91B309"/>
        <SolidColorBrush x:Key="FrustratedBrush" Color="#FF6EC7"/>
        <SolidColorBrush x:Key="FarewellBrush" Color="#9D4EDD"/>
        <SolidColorBrush x:Key="TextBrush" Color="#E8F4F8"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#B39DDB"/>
        <SolidColorBrush x:Key="OutcomeTextDarkBrush" Color="#1A0F2E"/>
        <SolidColorBrush x:Key="OutcomeTextLightBrush" Color="#FFFFF0"/>
        <SolidColorBrush x:Key="GridLineBrush" Color="#5A4080"/>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="{StaticResource GridLineBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource GridLineBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource InputBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Application.Resources>
</Application>
